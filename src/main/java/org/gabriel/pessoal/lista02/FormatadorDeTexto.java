package org.gabriel.pessoal.lista02;

/**
 * Exercício 07 — Sobrecarga de Métodos: Formatador de Texto.
 * <p>
 * Crie uma classe com um método {@code formatar} sobrecarregado: uma
 * versão que recebe só uma {@code String} (deixa tudo maiúsculo, por
 * exemplo), e outra que recebe uma {@code String} e um {@code int} (limita
 * o texto a esse tamanho máximo de caracteres).
 * <p>
 * <b>Pesquise:</b> o Java decide qual versão do método chamar baseado em
 * quê, exatamente? (Não é o nome — os dois métodos têm o mesmo nome.)
 */
public class FormatadorDeTexto {

    public static void main(String[] args) {
        // Implemente aqui.
    }
}